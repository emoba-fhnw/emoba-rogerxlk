package fhnw.emoba.modules.module07.flutter.data

import org.junit.Assert.assertEquals
import org.json.JSONObject
import org.junit.Test


internal class FlapTest {

    @Test
    fun testAll() {
/*
        //given
        val sender  = "Grossvater"
        val message = "Wo ist das Heidi?"
        val flap1 = Flap(sender, message)

        //when
        val json = JSONObject(flap1.asJsonString())
        val flap2 = Flap(json)

        //then
        assertEquals(sender,  json.getString("sender"))
        assertEquals(message, json.getString("message"))
        assertEquals(flap1, flap2)

*/
    }

}